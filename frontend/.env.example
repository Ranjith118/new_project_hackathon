# Copy to .env.local for local dev overrides
# In Render, set VITE_API_URL in the Static Site environment variables dashboard

# Point to your deployed backend URL (no trailing slash)
VITE_API_URL=https://maintenance-wizard-backend.onrender.com
